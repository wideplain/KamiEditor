<p id="notice"><%= notice %></p>
  <div class="raw">
    <div class="col s12 m6">
      <div class="card hoverable">
        <div class="card-content">
          <input id="input1" type="text" value="<%= @post.title %>">
          <form name="targetForm">
            <textarea type="text" class="materialize-textarea name="inputArea" value="<%= @post.body %>"><%= @post.body %></textarea>
            </textare>
            <button type="button" class="btn" data-clipboard-text="<%= @post.body %>">コピー(効きません)</button>
        </form>
        <div class="card-action">
          <%= link_to 'Edit', edit_post_path(@post) %>
          <%= link_to 'Back', posts_path %>
        </div>
      </div>
    </div>
  </div>
</div>
